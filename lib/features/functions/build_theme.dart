import 'package:flutter/material.dart';

ThemeData buildThemeApp() => ThemeData(
      fontFamily: 'Poppins',
      listTileTheme: const ListTileThemeData(
        contentPadding: EdgeInsets.all(1),
      ),
    );
